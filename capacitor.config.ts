import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.evaluacionunidad1',
  appName: 'evaluacionunidad1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
